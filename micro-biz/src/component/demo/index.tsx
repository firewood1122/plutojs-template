import React from 'react';
import './index.scss';

interface PropsType {
}
const demo = (props: PropsType) => {
  return (
    <div className="demo">
      <div className="icon"></div>
      <div>demo</div>
    </div>
  );
};

export default demo;
